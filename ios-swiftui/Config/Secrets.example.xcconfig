// Copy to Secrets.xcconfig and fill in values (or let CI write this file).
SUPABASE_URL = https://your-project.supabase.co
SUPABASE_ANON_KEY = your_anon_key_here

// Optional — soft update prompt (ไม่บังคับอัปเดต)
// Public TestFlight link, e.g. https://testflight.apple.com/join/XXXXXXXX
TESTFLIGHT_URL =
// Numeric App Store id if published (fallback open URL)
APP_STORE_ID =
